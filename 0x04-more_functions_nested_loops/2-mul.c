#include<stdio.h>
/**
 * mul - function that multiple two integers
 * @a: first int
 * @b: second int
 * Return: the result
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
