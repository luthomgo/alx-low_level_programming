#include<stdio.h>
/**
 * main - program that prints all possible combinations
 *
 * Return: Always 0
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		putchar(a);

	putchar('\n');
	putchar(',');

	return (0);
}
