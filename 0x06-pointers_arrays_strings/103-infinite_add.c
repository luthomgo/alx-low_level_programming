#include <stdio.h>
#include "main.h"
/**
 * infinite_add - function that adds two numbers
 * @n1: first number
 * @n2: second number
 * @r: buffer that the function use to store result
 * @size_r: buffer size
 * Return: Always 0
 */
char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	int a = 0;
	int b = 0;
	int f = 0;
	int d = 0;
	in}


